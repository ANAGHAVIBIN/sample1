git clone https://github.com/ANAGHAVIBIN/sample1.git

git and github

alternates:aws,bitbucket,gitlab

git add>>> and the file to the staging alternaitives(files ids being tracked by git)

git comit-m "message"  >>> to commit the changes in the tracked files

git push origin main >>> pushing the files to the main  branch in github respository


hellog

git and github

alternates:aws,bitbucket,gitlab

git add>>> and the file to the staging alternaitives(files ids being tracked by git)

git comit-m "message"  >>> to commit the changes in the tracked files

git push origin main >>> pushing the files to the main  branch in github respository


hellog